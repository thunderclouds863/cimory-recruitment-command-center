# COPY VALUES INTO STREAMLIT COMMUNITY CLOUD > APP SETTINGS > SECRETS.
# Do NOT commit a real secrets.toml to GitHub.

APP_NAME = "Recruitment Command Center"
AUTH_MODE = "local"

# Recommended for hosting: external PostgreSQL.
# Example shape only; use the connection string from your PostgreSQL provider.
DATABASE_URL = "postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require"
